package org.example.audio_ecommerce.entity.Enum;

public enum CategoryAttributeDataType {

    STRING,         // Text, short text
    NUMBER,         // Số (DECIMAL)
    BOOLEAN,        // true/false
    SELECT,         // Một lựa chọn cố định
    MULTI_SELECT,   // Nhiều lựa chọn
    LONG_TEXT       // Text dài (mô tả kỹ thuật)
}
