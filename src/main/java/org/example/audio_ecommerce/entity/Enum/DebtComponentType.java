package org.example.audio_ecommerce.entity.Enum;

public enum DebtComponentType {
    SHIP_DIFF,             // chênh lệch ship (delivered)
    RTO_FEE,               // phí quay đầu / không nhận (undelivered)
    RETURN_SHIPPING_FEE    // return_shipping_fees
}